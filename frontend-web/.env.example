# Frontend environment variables (copy to .env.local and adjust)
# API base URL used by BaseApiService (HTTPS recommended)
REACT_APP_API_URL=https://localhost:5001/api

# Sentry monitoring (optional)
# Obtain DSN from Sentry project settings; leave empty to disable
REACT_APP_SENTRY_DSN=
# 0 disables performance tracing; consider 0.1-0.3 in production
REACT_APP_SENTRY_TRACES_SAMPLE_RATE=0

# CRA build tweaks (optional)
# Uncomment to suppress source map warnings from dependencies
# GENERATE_SOURCEMAP=false
