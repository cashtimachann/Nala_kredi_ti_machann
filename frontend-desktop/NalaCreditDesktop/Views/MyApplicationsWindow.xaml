<Window x:Class="NalaCreditDesktop.Views.MyApplicationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mes demandes - Succursale" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner" Background="#F7F9FB">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#0F3D91" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📄" FontSize="22" Margin="0,0,8,0"/>
                    <StackPanel>
                        <TextBlock Text="Mes demandes" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="BranchText" Foreground="#E6ECFF"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="RefreshButton" Content="⟳ Rafraîchir" Margin="0,0,8,0"/>
                    <Button x:Name="CloseButton" Content="Fermer" Background="#DC3545" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filters + List -->
        <DockPanel Grid.Row="1" Margin="12,8,12,4">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
                <TextBlock Text="Recherche:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox x:Name="SearchTextBox" Width="200"/>
                <Button x:Name="SearchButton" Content="Rechercher" Margin="8,0,0,0"/>

                <Separator Margin="12,0,12,0" Width="1"/>

                <TextBlock Text="Statut:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="StatusCombo" Width="150" SelectedIndex="0">
                    <ComboBoxItem Content="Tous" Tag=""/>
                    <ComboBoxItem Content="Brouillon" Tag="Draft"/>
                    <ComboBoxItem Content="Soumise" Tag="Submitted"/>
                    <ComboBoxItem Content="En revue" Tag="UnderReview"/>
                    <ComboBoxItem Content="Approuvée" Tag="Approved"/>
                    <ComboBoxItem Content="Rejetée" Tag="Rejected"/>
                    <ComboBoxItem Content="Annulée" Tag="Cancelled"/>
                </ComboBox>

                <TextBlock Text="Type:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                <ComboBox x:Name="LoanTypeCombo" Width="200" SelectedIndex="0">
                    <ComboBoxItem Content="Tous" Tag=""/>
                    <ComboBoxItem Content="Crédit Commercial" Tag="commercial"/>
                    <ComboBoxItem Content="Crédit Agricole" Tag="agricultural"/>
                    <ComboBoxItem Content="Crédit Personnel" Tag="personal"/>
                    <ComboBoxItem Content="Crédit d'Urgence" Tag="emergency"/>
                    <ComboBoxItem Content="Crédit Loyer" Tag="creditloyer"/>
                    <ComboBoxItem Content="Crédit Auto" Tag="creditauto"/>
                    <ComboBoxItem Content="Crédit Moto" Tag="creditmoto"/>
                    <ComboBoxItem Content="Crédit Scolaire" Tag="creditscolaire"/>
                    <ComboBoxItem Content="Crédit Professionnel" Tag="creditprofessionnel"/>
                    <ComboBoxItem Content="Crédit d'Appui" Tag="creditappui"/>
                    <ComboBoxItem Content="Crédit Hypothécaire" Tag="credithypothecaire"/>
                </ComboBox>

                <TextBlock Text="Par page:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                <ComboBox x:Name="PageSizeCombo" Width="80" SelectedIndex="1">
                    <ComboBoxItem Content="20"/>
                    <ComboBoxItem Content="50"/>
                    <ComboBoxItem Content="100"/>
                    <ComboBoxItem Content="200"/>
                </ComboBox>

                <Button x:Name="ApplyFiltersButton" Content="Appliquer" Margin="12,0,0,0"/>
            </StackPanel>
            <DataGrid x:Name="ApplicationsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                      MouseDoubleClick="ApplicationsDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Demande" Binding="{Binding ApplicationNumber}" Width="120"/>
                    <DataGridTextColumn Header="Client" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Montant" Binding="{Binding RequestedAmount, StringFormat=N0}" Width="120"/>
                    <DataGridTextColumn Header="Durée (mois)" Binding="{Binding RequestedDurationMonths}" Width="120"/>
                    <DataGridTextColumn Header="Type de crédit" Binding="{Binding LoanTypeDisplay}" Width="160"/>
                    <DataGridTextColumn Header="Statut" Binding="{Binding StatusDisplay}" Width="140"/>
                    <DataGridTextColumn Header="Créée" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTemplateColumn Header="Actions" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="👁 Voir" Click="ViewDetails_Click" Tag="{Binding Id}" 
                                        Padding="6,2" Background="#0D6EFD" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <!-- Pagination -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="0,8,0,0" HorizontalAlignment="Right">
                <Button x:Name="PrevPageButton" Content="◀ Précédent" Margin="0,0,8,0"/>
                <TextBlock x:Name="PageInfoText" VerticalAlignment="Center"/>
                <Button x:Name="NextPageButton" Content="Suivant ▶" Margin="8,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Footer -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
