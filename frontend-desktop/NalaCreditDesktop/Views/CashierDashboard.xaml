<Window x:Class="NalaCreditDesktop.Views.CashierDashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:NalaCreditDesktop.Converters"
    xmlns:views="clr-namespace:NalaCreditDesktop.Views"
        Title="Dashboard Caissier - Nala Kredi" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Convertisseurs -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:CurrencyFormatConverter x:Key="CurrencyFormatConverter"/>
        <!-- Styles personnalisés -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F8F9FA" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="BorderBrush" Value="#E1E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#D1D5DB" Direction="270" ShadowDepth="5" BlurRadius="10" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#4A90E2" Offset="0"/>
                        <GradientStop Color="#357ABD" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#357ABD" Offset="0"/>
                                            <GradientStop Color="#2C5AA0" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#2C5AA0" Offset="0"/>
                                            <GradientStop Color="#1E3F73" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
        
        <Style x:Key="CurrencyHTGStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueTextStyle}">
            <Setter Property="Foreground" Value="#27AE60"/>
        </Style>
        
        <Style x:Key="CurrencyUSDStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueTextStyle}">
            <Setter Property="Foreground" Value="#3498DB"/>
        </Style>
        
        <Style x:Key="AlertWarningStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF3CD"/>
            <Setter Property="BorderBrush" Value="#FFC107"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <Style x:Key="AlertCriticalStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8D7DA"/>
            <Setter Property="BorderBrush" Value="#DC3545"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="35"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête avec actions rapides -->
        <Border Grid.Row="0" Padding="20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2C3E50" Offset="0"/>
                    <GradientStop Color="#34495E" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Titre et informations session -->
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="Dashboard Caissier" 
                               FontSize="24" FontWeight="Bold" 
                               Foreground="White"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="{Binding CashierName}" 
                                   Foreground="#BDC3C7" FontSize="14"/>
                        <TextBlock Text=" • " Foreground="#BDC3C7" FontSize="14"/>
                        <TextBlock Text="{Binding SessionStartTime, StringFormat='Ouvert à {0:HH:mm}'}" 
                                   Foreground="#BDC3C7" FontSize="14"/>
                        <TextBlock Text=" • " Foreground="#BDC3C7" FontSize="14"/>
                        <TextBlock Text="{Binding CurrentTime, StringFormat='Maintenant: {0:HH:mm:ss}'}" 
                                   Foreground="#BDC3C7" FontSize="14"/>
                    </StackPanel>
                </StackPanel>

                <!-- Statut de session -->
                <Border Grid.Column="1" 
                        Background="{Binding SessionStatusColor}" 
                        CornerRadius="20" 
                        Padding="15,8" 
                        Margin="20,0">
                    <TextBlock Text="{Binding SessionStatusText}" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               FontSize="14"/>
                </Border>

                <!-- Actions rapides -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource ActionButtonStyle}" 
                            Click="OpenQuickDeposit_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Dépôt"/>
                        </StackPanel>
                    </Button>
                    
                        <Button Style="{StaticResource ActionButtonStyle}" 
                            Click="OpenQuickWithdrawal_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➖" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Retrait"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Command="{Binding ExchangeOperationCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Change"/>
                        </StackPanel>
                    </Button>
                    
                        <Button Style="{StaticResource ActionButtonStyle}" 
                            Background="#9B59B6"
                            Command="{Binding ToggleTransactionModuleCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding TransactionModuleToggleLabel}"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Background="#E74C3C"
                            Command="{Binding CloseCashierCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔒" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Clôture"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenu principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Alertes -->
                <StackPanel Grid.Row="0" x:Name="AlertsPanel">
                    <!-- Message session fermée -->
                    <Border Background="#FFF3E0" 
                            BorderBrush="#FF9800" 
                            BorderThickness="2" 
                            CornerRadius="5" 
                            Padding="15" 
                            Margin="5"
                            x:Name="SessionClosedAlert">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SessionStatusText}" Value="CAISSE FERMÉE">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="ℹ️" FontSize="20" Margin="0,0,10,0"/>
                                <TextBlock Text="La session de caisse est fermée. Cliquez pour ouvrir une nouvelle session." 
                                           FontWeight="SemiBold" 
                                           Foreground="#E65100"
                                           FontSize="14"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button Grid.Column="1"
                                    Command="{Binding OpenCashSessionCommand}"
                                    Background="#FF9800"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="20,10"
                                    FontWeight="Bold"
                                    FontSize="14"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F57C00"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                🔓 OUVRIR SESSION
                            </Button>
                        </Grid>
                    </Border>
                    
                    <!-- Alerte warning -->
                    <Border Style="{StaticResource AlertWarningStyle}" 
                            Visibility="{Binding HasWarningAlerts, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding WarningMessage}" FontWeight="SemiBold" Foreground="#856404"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Alerte critique -->
                    <Border Style="{StaticResource AlertCriticalStyle}" 
                            Visibility="{Binding HasCriticalAlerts, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🚨" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding CriticalMessage}" FontWeight="SemiBold" Foreground="#721C24"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Première ligne : Soldes et Résumé du Jour -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Suivi soldes caisse -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="💰 Soldes de Caisse" Style="{StaticResource HeaderTextStyle}"/>
                            
                            <!-- Soldes actuels -->
                            <Grid Margin="0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- HTG -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="🇭🇹 HTG:" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="16"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" 
                                           Text="{Binding CurrentBalanceHTG, StringFormat='{}{0:N0}'}" 
                                           Style="{StaticResource CurrencyHTGStyle}" 
                                           HorizontalAlignment="Right" FontSize="18"/>

                                <!-- USD -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="🇺🇸 USD:" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="16"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" 
                                           Text="{Binding CurrentBalanceUSD, StringFormat='${0:N2}'}" 
                                           Style="{StaticResource CurrencyUSDStyle}" 
                                           HorizontalAlignment="Right" FontSize="18"/>

                                <Separator Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10"/>

                                <!-- Détails -->
                                <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Solde d'ouverture HTG:"/>
                                        <TextBlock Grid.Column="1" Text="{Binding OpeningBalanceHTG, StringFormat='{}{0:N0}'}" FontWeight="SemiBold"/>
                                    </Grid>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Solde d'ouverture USD:"/>
                                        <TextBlock Grid.Column="1" Text="{Binding OpeningBalanceUSD, StringFormat='${0:N2}'}" FontWeight="SemiBold"/>
                                    </Grid>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Total entrées:"/>
                                        <TextBlock Grid.Column="1" Text="{Binding TotalIncoming, StringFormat='{}{0:N0}'}" Foreground="#27AE60" FontWeight="SemiBold"/>
                                    </Grid>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Total sorties:"/>
                                        <TextBlock Grid.Column="1" Text="{Binding TotalOutgoing, StringFormat='{}{0:N0}'}" Foreground="#E74C3C" FontWeight="SemiBold"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <!-- Indicateur d'équilibre -->
                            <Border Background="{Binding BalanceIndicatorColor}" 
                                    CornerRadius="5" 
                                    Padding="10" 
                                    Margin="0,10,0,0">
                                <TextBlock Text="{Binding BalanceIndicatorText}" 
                                           Foreground="White" 
                                           FontWeight="SemiBold" 
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Résumé du Jour -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📊 Résumé du Jour" Style="{StaticResource HeaderTextStyle}"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Dépôts -->
                                <Border Grid.Column="0" Margin="0,0,10,0" Background="#D4EDDA" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="💵 Dépôts" FontWeight="Bold" Foreground="#155724" Margin="0,0,0,10" FontSize="16"/>
                                        <TextBlock Text="{Binding DepositsCount, StringFormat='Nombre: {0}'}" FontSize="14" Foreground="#155724"/>
                                        <TextBlock Text="{Binding DepositsAmountHTG, StringFormat='HTG: {0:N0}'}" FontSize="14" Foreground="#155724" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding DepositsAmountUSD, StringFormat='USD: ${0:N2}'}" FontSize="14" Foreground="#155724" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>

                                <!-- Retraits -->
                                <Border Grid.Column="1" Background="#F8D7DA" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="💸 Retraits" FontWeight="Bold" Foreground="#721C24" Margin="0,0,0,10" FontSize="16"/>
                                        <TextBlock Text="{Binding WithdrawalsCount, StringFormat='Nombre: {0}'}" FontSize="14" Foreground="#721C24"/>
                                        <TextBlock Text="{Binding WithdrawalsAmountHTG, StringFormat='HTG: {0:N0}'}" FontSize="14" Foreground="#721C24" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding WithdrawalsAmountUSD, StringFormat='USD: ${0:N2}'}" FontSize="14" Foreground="#721C24" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Separator Margin="0,10"/>

                            <!-- Opérations de change -->
                            <TextBlock Text="🔄 Opérations de Change" FontWeight="Bold" Foreground="#3498DB" Margin="0,15,0,10" FontSize="16"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Column="0" Margin="0,0,10,0" Background="#D1ECF1" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="Vente USD" FontWeight="SemiBold" Foreground="#0C5460" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding UsdSalesAmount, StringFormat='HTG: {0:N0}'}" FontSize="14" Foreground="#0C5460" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Grid.Column="1" Background="#D4EDDA" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="Achat USD" FontWeight="SemiBold" Foreground="#155724" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding UsdPurchaseAmount, StringFormat='USD: ${0:N2}'}" FontSize="14" Foreground="#155724" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Deuxième ligne : Dernières transactions et actions -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Liste des dernières transactions -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="🕒 Dernières Transactions" Style="{StaticResource HeaderTextStyle}"/>
                                <Button HorizontalAlignment="Right" 
                                        Content="Actualiser" 
                                        Padding="10,5"
                                        Background="Transparent"
                                        BorderBrush="#BDC3C7"
                                        Command="{Binding RefreshTransactionsCommand}"/>
                            </Grid>
                            
                            <DataGrid ItemsSource="{Binding RecentTransactions}" 
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      Style="{StaticResource ModernDataGridStyle}"
                                      Height="300"
                                      Margin="0,10">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Heure" 
                                                        Binding="{Binding Time, StringFormat='{}{0:HH:mm:ss}'}" 
                                                        Width="80"/>
                                    <DataGridTextColumn Header="Type" 
                                                        Binding="{Binding Type}" 
                                                        Width="80"/>
                                    <DataGridTextColumn Header="Client/Compte" 
                                                        Binding="{Binding ClientAccount}" 
                                                        Width="*"/>
                                    <DataGridTextColumn Header="Montant" 
                                                        Binding="{Binding Amount}" 
                                                        Width="120"/>
                                    <DataGridTemplateColumn Header="Statut" Width="100">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding StatusColor}" 
                                                        CornerRadius="3" 
                                                        Padding="5,2">
                                                    <TextBlock Text="{Binding Status}" 
                                                               Foreground="White" 
                                                               FontSize="11" 
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- Actions et consultation -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="🔍 Actions Rapides" Style="{StaticResource HeaderTextStyle}"/>
                            
                            <!-- Consultation compte -->
                            <StackPanel Margin="0,10">
                                <TextBlock Text="Consultation Compte" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="AccountSearchBox" 
                                         Text="{Binding AccountSearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="10,8"
                                         FontSize="14"
                                         Margin="0,0,0,5"/>
                                <Button Content="👤 Consulter" 
                                        Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding ConsultAccountCommand}"/>
                            </StackPanel>

                            <Separator Margin="0,15"/>

                            <!-- Génération rapport -->
                            <StackPanel Margin="0,10">
                                <TextBlock Text="Rapports" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <Button Content="📊 Rapport Journalier" 
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#17A2B8"
                                        Command="{Binding GenerateDailyReportCommand}"/>
                                <Button Content="📈 Export Excel" 
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#28A745"
                                        Command="{Binding ExportToExcelCommand}"/>
                            </StackPanel>

                            <Separator Margin="0,15"/>

                            <!-- Autres actions -->
                            <StackPanel Margin="0,10">
                                <TextBlock Text="Autres Actions" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <Button Content="⚙️ Configuration" 
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#6C757D"
                                        Command="{Binding ConfigurationCommand}"/>
                                <Button Content="📞 Support" 
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#FFC107"
                                        Command="{Binding SupportCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Module complet de gestion des transactions -->
                <Border Grid.Row="3"
                        Style="{StaticResource CardStyle}"
                        Padding="0"
                        Visibility="{Binding IsTransactionModuleVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <views:TransactionView ShowHeaderQuickActions="False"/>
                </Border>

                <!-- Historique des changes (caissier - aujourd'hui) -->
                <Border Grid.Row="4" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="🔄 Historique des Changes (Aujourd'hui)" Style="{StaticResource HeaderTextStyle}"/>
                            <Button HorizontalAlignment="Right"
                                    Content="Actualiser"
                                    Padding="10,5"
                                    Background="Transparent"
                                    BorderBrush="#BDC3C7"
                                    Command="{Binding RefreshTransactionsCommand}"/>
                        </Grid>

                        <DataGrid ItemsSource="{Binding ExchangeHistory}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Style="{StaticResource ModernDataGridStyle}"
                                  Height="260"
                                  Margin="0,10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Heure"
                                                    Binding="{Binding Time, StringFormat='{}{0:HH:mm:ss}'}"
                                                    Width="90"/>
                                <DataGridTextColumn Header="N°"
                                                    Binding="{Binding TransactionNumber}"
                                                    Width="120"/>
                                <DataGridTextColumn Header="Paire"
                                                    Binding="{Binding Pair}"
                                                    Width="100"/>
                                <DataGridTextColumn Header="Montant Source"
                                                    Binding="{Binding FromAmount, StringFormat='{}{0:N2}'}"
                                                    Width="140"/>
                                <DataGridTextColumn Header="Taux"
                                                    Binding="{Binding ExchangeRate, StringFormat='{}{0:N2}'}"
                                                    Width="100"/>
                                <DataGridTextColumn Header="Montant Dest."
                                                    Binding="{Binding ToAmount, StringFormat='{}{0:N2}'}"
                                                    Width="140"/>
                                <DataGridTextColumn Header="Net"
                                                    Binding="{Binding NetAmount, StringFormat='{}{0:N2}'}"
                                                    Width="120"/>
                                <DataGridTextColumn Header="Client"
                                                    Binding="{Binding Customer}"
                                                    Width="*"/>
                                <DataGridTextColumn Header="Statut"
                                                    Binding="{Binding Status}"
                                                    Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Barre de statut -->
        <Border Grid.Row="2" Background="#34495E" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="💼" Foreground="White" FontSize="14" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding BranchName}" Foreground="White" FontSize="14"/>
                    <TextBlock Text=" • " Foreground="#BDC3C7" FontSize="14"/>
                    <TextBlock Text="{Binding SessionId, StringFormat='Session: {0}'}" Foreground="#BDC3C7" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <Ellipse Width="12" Height="12" Fill="{Binding ConnectionStatusColor}" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="White" FontSize="14"/>
                </StackPanel>

                <TextBlock Grid.Column="2" 
                           Text="{Binding LastTransactionTime, StringFormat='Dernière transaction: {0:HH:mm:ss}'}" 
                           Foreground="#BDC3C7" 
                           FontSize="14"/>
            </Grid>
        </Border>
    </Grid>
</Window>